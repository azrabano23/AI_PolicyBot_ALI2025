services:
  - type: web
    name: ali2025-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 60 test_app:app
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
